## Questions on Moving Forward:

* How's the performance on real Lichess data? Can you query 5M positions in reasonable time?

Hopefully yes.

* What false positives are you seeing? When you run a script, what matches feel "wrong" semantically?

Currently in the process of running the scripts through a data flow pipeline while previewing outputs.

* What patterns have you tried to write that the language can't express? That's your limiting factor.

Not exactly focused on this aspect to find the boundaries yet.

* The orchestrator + filtering — is the current output format useful for building your pattern library?

Hopefully yes, but needs careful testing and put into real practice still.